# ══════════════════════════════════════════════════════════════
#  GST & e-Invoicing Assistant — Python Backend Requirements
#  Install with: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── Core HTTP / API ──────────────────────────────────────────
requests>=2.31.0            # HTTP calls to NIC IRP / EWB APIs
httpx>=0.26.0               # Async HTTP (for FastAPI integration)
aiohttp>=3.9.0              # Async HTTP alternative

# ── Web Framework (optional backend) ─────────────────────────
fastapi>=0.109.0            # REST API server
uvicorn[standard]>=0.27.0   # ASGI server for FastAPI
python-dotenv>=1.0.0        # Load .env variables
pydantic>=2.5.0             # Data validation

# ── Document Processing ───────────────────────────────────────
pypdf2>=3.0.0               # PDF text extraction
python-docx>=1.1.0          # Word document processing
openpyxl>=3.1.2             # Excel (.xlsx) processing
pandas>=2.1.0               # CSV / tabular data processing

# ── GST / e-Invoice Utilities ────────────────────────────────
# (Add NIC-certified SDK if available from your IRP provider)
# cleartax-einvoice-sdk      # ClearTax e-Invoice SDK (if using ClearTax IRP)

# ── Security ──────────────────────────────────────────────────
cryptography>=42.0.0        # For token signing / verification
PyJWT>=2.8.0                # JWT token handling

# ── Logging & Monitoring ──────────────────────────────────────
loguru>=0.7.2               # Structured logging

# ── Testing ───────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0               # Test HTTP client
